{
  "ConnectionStrings": {
    "DefaultConnection": "server=${env:DATABASE_URL};user=${env:DATABASE_USER};password=${env:DATABASE_PASSWORD};database=${env:DATABASE_NAME}"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
